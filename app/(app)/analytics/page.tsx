import { ComingSoon } from "@/components/layout/coming-soon";

export default function AnalyticsPage() {
  return (
    <ComingSoon
      title="Analytics"
      description="Strength, volume, consistency, and body-metric charts will arrive once the training history has a richer set of days."
    />
  );
}
