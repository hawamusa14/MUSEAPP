import { ComingSoon } from "@/components/layout/coming-soon";

export default function CalendarPage() {
  return (
    <ComingSoon
      title="Calendar"
      description="A monthly studio calendar will gather workouts, nutrition, steps, photos, and rest days into one quiet view."
    />
  );
}
