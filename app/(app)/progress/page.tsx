import { ComingSoon } from "@/components/layout/coming-soon";

export default function ProgressPage() {
  return (
    <ComingSoon
      title="Progress"
      description="Weight, measurements, and secure progress photos will sit in this gallery. Image files will be stored in object storage, not in PostgreSQL."
    />
  );
}
